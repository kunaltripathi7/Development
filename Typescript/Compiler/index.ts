/* tsc --watch -> recompile on save
tsc -> all files in dir compile
add nodemodules in exclude if override
outDir
strict :
nullChecks -> null/undefined -> are distinct types will get error if assign if not included.
noEmitError -> don't compile into js if it has error*/
